name: Cookie Guardian

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  guardian:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        shard: [0, 1, 2]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install WARP
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp

      - name: Cache browser profiles
        uses: actions/cache@v4
        with:
          path: data/profiles
          key: cookie-guardian-profiles-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            cookie-guardian-profiles-${{ runner.os }}-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          patchright install chromium

      - name: Create data directories
        run: mkdir -p data/profiles data/har data/traces logs

      - name: Initialize database
        run: python scripts/init_db.py

      - name: Connect WARP
        continue-on-error: true
        run: |
          warp-cli --accept-tos registration new
          warp-cli --accept-tos connect
          sleep 2
          warp-cli --accept-tos status

      - name: Run orchestrator
        env:
          CG_GITHUB_TOKEN: ${{ secrets.CG_GITHUB_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          # Optional: unified platform credentials (falls back to USER_CREDENTIALS_{PLATFORM})
          USER_CREDENTIALS: ${{ secrets.USER_CREDENTIALS }}
          # Optional: fallback credentials used when no platform-specific credentials exist
          CG_FALLBACK_USERNAME: ${{ secrets.CG_FALLBACK_USERNAME }}
          CG_FALLBACK_PASSWORD: ${{ secrets.CG_FALLBACK_PASSWORD }}
        run: python -m src.orchestrator 2>&1 | tee logs/run.log

      - name: Disconnect WARP
        if: always()
        run: warp-cli --accept-tos disconnect || true

      - name: Redact logs before upload
        if: always()
        run: |
          # Redact any potentially sensitive data from logs
          sed -i 's/"value":"[^"]*"/"value":"[REDACTED]"/g' logs/run.log || true
          sed -i 's/Cookie: [^[:space:]]*/Cookie: [REDACTED]/g' logs/run.log || true
          sed -i 's/Authorization: Bearer [^[:space:]]*/Authorization: Bearer [REDACTED]/g' logs/run.log || true

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cookie-guardian-logs-shard-${{ matrix.shard }}
          path: logs/run.log
          retention-days: 7
